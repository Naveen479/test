pipeline {
  agent {
    label 'docker-slave'
  }
  stages {
    stage('Test') {
      steps {
        sh '''
          sudo apt update -y
          ls -al
          pwd
          
          '''
        
      }
    }
  }
}
