pipeline {
  agent {
    label 'docker-slave'
  }
  stages {
    stage('Test') {
      steps {
        sh '''
          apt update -y
          ls -al
          pwd
          
          '''
        
      }
    }
  }
}
